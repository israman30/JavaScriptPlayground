 // Initialized class
class Human {
    // properties
    constructor(){
        this.arms = 2;
        this.head = 1;
        this.myName = name;
    }
}

// Instances of a class

const species = new Human('Israel');

console.log('The human has ' + species.arms + ' arms');

console.log('The human is ' + species.myName);